// import { notBlank } from '../Shared/Validators'
// import { SearchFormValues } from '../../../interfaces'

// export const validate = ({ values }: { values: SearchFormValues }) =>
// 	notBlank(values, ['term'], undefined)
